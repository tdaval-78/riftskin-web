# RiftSkin Web

Static web project deployed on Vercel.

## Paths

- `/auth/callback`: Supabase auth callback (email confirmation + password reset UI)
- `/auth/email-confirmed`: static confirmation page

## Deploy

This repository is connected to Vercel project `web`.
Every push to `main` triggers an automatic production deployment.
